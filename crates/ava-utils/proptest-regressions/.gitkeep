# Committed proptest regression corpus for ava-utils (specs/02 §4.1).
# proptest appends `*.txt` failure seeds here; they are committed so a once-seen
# counterexample is replayed forever. Placeholder so the dir is tracked.
