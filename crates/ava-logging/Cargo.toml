[package]
name = "ava-logging"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
description = "Logging level/format model + tracing/OTel wiring (tier X / X.18, specs/18 §5–§6)."

[dependencies]
thiserror = { workspace = true }
# tracing = { workspace = true }  # wired in by X.18 with the format/subscriber layers

[lints]
workspace = true
