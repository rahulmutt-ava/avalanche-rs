load("@rules_rust//rust:defs.bzl", "rust_library", "rust_test")

rust_library(
    name = "ava_logging",
    srcs = [
        "src/level.rs",
        "src/lib.rs",
    ],
    compile_data = ["Cargo.toml"],
    visibility = ["//visibility:public"],
    deps = ["@crates//:thiserror"],
)

rust_test(
    name = "ava_logging_test",
    compile_data = ["Cargo.toml"],
    crate = ":ava_logging",
)
