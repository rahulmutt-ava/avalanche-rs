# `ava-secp256k1fx` porting notes

Port of Go `github.com/ava-labs/avalanchego/vms/secp256k1fx` (M3.19, specs
07 §4). Source files cited below are relative to the pinned `../avalanchego`
tree.

## Provenance of source mapping

| Rust | Go source | Notes |
|------|-----------|-------|
| `types::OutputOwners` (+`verify`, codec) | `output_owners.go` | `Verify` branch order/sentinels reproduced exactly (`ErrOutputUnspendable`/`ErrOutputUnoptimized`/`ErrAddrsNotSortedUnique`). |
| `types::Input` (+`cost`, `verify`) | `input.go` | `CostPerSignature=1000`, `Cost = len*1000` (checked). |
| `types::TransferInput` | `transfer_input.go` | typeID 0. |
| `types::MintOutput` | `mint_output.go` | typeID 1; `verify::State`. |
| `types::TransferOutput` | `transfer_output.go` | typeID 2; `verify::State`. |
| `types::Credential` | `credential.go` | typeID 4; 65-byte `[r‖s‖v]` sigs. |
| `fx::Fx::verify_credentials` | `fx.go` `VerifyCredentials` | bit-for-bit; see below. |
| typeID 3 `MintOperation` | `mint_operation.go` | **not ported** (no consumer yet); reserved as `TypeId::MintOperation = 3` only. |

`MintOperation` / `MintOutput.Equals` mint-spend path
(`Fx.VerifyOperation`/`verifyOperation`), `VerifyTransfer`/`VerifySpend`
(`utxo.Amt == in.Amt` ⇒ `ErrMismatchedAmounts`), `CreateOutput`, `keychain.go`,
and the JSON `MarshalJSON` formatting are **out of M3.19 scope** (the fx
framework `FxInstance` lands at M3.20; the amount/mint checks belong to the
X-Chain at M4). This crate ships the codec types + the `verify_credentials`
multisig gate only.

## Error model

The fx sentinels (`ErrTimelocked`, `ErrTooManySigners`, `ErrTooFewSigners`,
`ErrInputCredentialSignersMismatch`, `ErrInputOutputIndexOutOfBounds`,
`ErrWrongSig`, `ErrOutputUnspendable`, `ErrOutputUnoptimized`,
`ErrAddrsNotSortedUnique`, the wrong-type set, …) already live on the shared
`ava_vm::error::Error` enum (added at M3.14, per the plan note "the fx wrong-type
set re-exported by `ava-secp256k1fx`"). `error.rs` re-exports `Error`/`Result`
from `ava-vm` so the fx framework and downstream VMs see one error type.

The syntactic-validation sentinels with **no `ava-vm` analog** — `ErrNilInput`
(N/A: a `&Input` is never nil in Rust), `ErrNoValueInput`, `ErrNoValueOutput`,
`ErrInputIndicesNotSortedUnique`, `ErrNilCredential` — are surfaced via
`Error::InvalidComponent(<go sentinel text>)`.

## `verify_credentials` (07 §4.3) — bit-for-bit

Reproduces `fx.go`'s `VerifyCredentials` exactly:

1. `owner.locktime > clock.unix()` ⇒ `Timelocked` (Go `Clock.Unix()` — whole
   seconds, clamped ≥ 0).
2. `threshold < num_sigs` ⇒ `TooManySigners`; `threshold > num_sigs` ⇒
   `TooFewSigners` (two branches, same order as Go).
3. `num_sigs != cred.sigs.len()` ⇒ `InputCredentialSignersMismatch`.
4. `!bootstrapped` ⇒ skip signature verification (`Ok(())`).
5. For each `(index, sig)`: `index >= len(addrs)` ⇒
   `InputOutputIndexOutOfBounds`; recover `pk` from
   `sha256(unsigned_tx.bytes())` (Go `hashing.ComputeHash256`) and require
   `addrs[index] == ripemd160(sha256(compressed_pk))` (`pk.address()`), else
   `WrongSig`. `cred.sigs[i]` pairs positionally with `sig_indices[i]`.

Signature recovery: `ava_crypto::secp256k1::PublicKey::recover_from_hash`
(low-S enforced before recovery, `00` §4.3). The `Fx` carries no recover cache
(the Go `RecoverCache` is a pure LRU optimization; recovery is deterministic).

## Golden vectors — `tests/vectors/secp256k1fx/vectors.json`

Generated by a scratch Go program built **against the pinned `../avalanchego`
tree** (run in a temp dir, not committed) and captured into `vectors.json`. The
program:

* `codec` array — `codec.Manager.Marshal(0, &concrete)` for each type (2-byte
  codec-version prefix `0x0000` + the `serialize:"true"` fields, no typeID —
  the same bytes the Go unit tests assert). The `transfer_input`,
  `transfer_output`, and `credential` vectors reproduce the exact `expected`
  byte arrays in `transfer_input_test.go` / `transfer_output_test.go` /
  `credential_test.go`.
* `multisig` array — real RFC6979 signatures over a fixed unsigned-tx byte
  string, with deterministic owner keys. **Each case's expected outcome was
  validated by driving the real `secp256k1fx.Fx.VerifyCredentials` in the
  generator before capture** (the program panics on any divergence), covering:
  threshold 1/2/3 accept, matured locktime, `ErrTimelocked`,
  `ErrTooManySigners`, `ErrTooFewSigners`, `ErrWrongSig`,
  `ErrInputOutputIndexOutOfBounds`, and the bootstrap signature-skip.

RFC6979 deterministic signing means the Rust `PrivateKey::sign_hash` reproduces
Go's signatures bit-for-bit, so the proptest (`prop::multisig_verify`) signs
locally rather than from captured sigs and still matches Go semantics.

## Tests

* `golden_codec.rs` — 8 byte-exact round-trip tests vs the committed vectors.
* `prop_multisig.rs` — `go_multisig_vectors` (10 Go-validated cases),
  `output_owners_verify_table` (`OutputUnspendable`/`OutputUnoptimized`/
  `AddrsNotSortedUnique`), and `prop::multisig_verify` (128 proptest cases:
  accept iff exactly `threshold` valid owner sigs at sorted-unique matured
  indices; reject over/under-sign, reordered, OOB, wrong-sig, premature).

## M3.20 — `FxInstance` registration (`instance::Secp256k1Fx`)

Ports the fx-framework surface of `vms/secp256k1fx/fx.go` (`Initialize`,
`Bootstrapping`, `Bootstrapped`, `VerifyTransfer`/`VerifySpend`,
`VerifyPermission`, `VerifyOperation`, `CreateOutput`) onto the new
`ava_vm::fx::FxInstance` trait (specs 07 §4.1). The `&dyn Any` boundary mirrors
Go's `interface{}` + type-assert: each method `downcast_ref`s its arguments and
maps a downcast miss to the exact Go sentinel (`ErrWrongInputType`,
`ErrWrongCredentialType`, `ErrWrongUTXOType`, `ErrWrongOwnerType`,
`ErrWrongOpType`), then delegates to `Fx::verify_credentials`.

| Go (`fx.go`) | Rust (`instance.rs`) | Notes |
|---|---|---|
| `Initialize` (`RegisterType` ×5) | `Secp256k1Fx::initialize` | Registers `TransferInput`/`MintOutput`/`TransferOutput`/`MintOperation`/`Credential` **by name in that order** into the host's `ava_vm::fx::CodecRegistry`. |
| `InitializeVM` (`vmIntf.(VM)` ⇒ `ErrWrongVMType`) | folded into `FxVm` param | The host VM type is fixed by the `Arc<dyn FxVm>` signature, so the `ErrWrongVMType` downcast has no Rust analog. |
| `VerifyTransfer` → `VerifySpend` | `verify_transfer` → `verify_spend` | `verify::All(utxo, in, cred)` then `utxo.amt != in.amt` ⇒ `MismatchedAmounts`, then `verify_credentials`. |
| `VerifyPermission` | `verify_permission` | input downcasts to `Input` (not `TransferInput`), `verify::All(in, cred, owner)`, then `verify_credentials`. |
| `CreateOutput` | `create_output` | owner ⇒ `OutputOwners`, `owner.verify()`, returns `Arc<TransferOutput>` as `Arc<dyn verify::State>`. |
| `VerifyOperation`/`verifyOperation` | `verify_operation` | **partial deferral, see below.** |

### Deferral — `verify_operation` (mint)

`MintOperation` (typeID 3) is still not a Rust codec type (M3.19 reserved the
discriminant only), and `MintOutput.Equals(op.MintOutput.OutputOwners)` /
`op.MintInput` cannot be expressed without it. `verify_operation` therefore
downcasts the op against a private never-constructed `MintOperationUnported`
marker, so it faithfully returns `ErrWrongOpType` for any argument — matching Go
for every input *except* a real `*MintOperation`, which does not yet exist in
Rust. The full mint check (`verify::All(op, cred, utxo)` → `ErrWrongMintCreated`
→ `verify_credentials(&op.MintInput, …)`) lands when `MintOperation` is ported
(X-Chain, M4). No current consumer reaches the mint path.

### Trait/type unification

`UnsignedTx` moved to its canonical owner `ava_vm::fx` (the fx framework) so the
`FxInstance` surface and `Fx::verify_credentials` share one tx-bytes trait; the
blanket `impl UnsignedTx for Vec<u8>`/`&[u8]` (used by the multisig proptests)
moved there too (orphan-rule: the impls must live with the trait).
`ava_secp256k1fx::fx::UnsignedTx` is now a re-export of `ava_vm::fx::UnsignedTx`.

### `FxVm` host surface

`ava_vm::fx::FxVm` exposes `codec_registry()` + `clock()`. Go's `VM` interface
also has `Logger()`; there is **no `Logger` type in the workspace yet**, so it is
omitted from `FxVm`. **Recommended spec note** (07 §4.1): drop `logger: Logger`
from the `FxVm` sketch until a logging facade lands, or annotate it as deferred.
The `CodecRegistry` surface is modelled as a `register_type(name) -> Result<u32>`
trait (returns the assigned sequential typeID) rather than the generic Go
`RegisterType(interface{})`, since Rust codec typeIDs are pinned on the enum at
derive time (`ava-codec` §`linearcodec`); registration here records ordering for
host-side assertion.
