[package]
name = "ava-secp256k1fx"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
publish.workspace = true
description = "secp256k1 feature extension: OutputOwners/Input/Transfer{In,Out}put/MintOutput/Credential codec types + multisig verify_credentials (port of vms/secp256k1fx) (M3 / specs 07 §4)."

[dependencies]
ava-types = { path = "../ava-types" }
ava-codec = { path = "../ava-codec" }
ava-crypto = { path = "../ava-crypto" }
ava-utils = { path = "../ava-utils" }
ava-vm = { path = "../ava-vm" }

[dev-dependencies]
hex = { workspace = true }
serde = { workspace = true, features = ["derive"] }
serde_json = { workspace = true }
proptest = { workspace = true }
assert_matches = { workspace = true }

[lints]
workspace = true
