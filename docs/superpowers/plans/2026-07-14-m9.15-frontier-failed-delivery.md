# M9.15 `*Failed` delivery fix — implementation plan

> Sub-cycle of `2026-07-05-m9.15-live-mixed-network-firewood-v07.md` Task 8.
> Design: `docs/superpowers/specs/2026-07-14-m9.15-frontier-failed-delivery-design.md`.
> Branch: `m9.15-live-mixed-net-v07` (continue in place — the parent plan's live
> re-run consumes this fix directly).

**Goal:** every registered request yields exactly one of {response, `*Failed`}
delivered to the engine — immediately for unsent messages, within the adaptive
timeout for silent loss — so live 5-beacon frontier discovery cannot stall.

## Global constraints

- TDD strictly: each fix lands only after a RED test demonstrates the defect.
- Go oracle for parity questions: `~/avalanchego/snow/networking/sender/sender.go`
  (+ `router/`, `timeout/`). Cite the Go lines in code comments only where the
  Rust shape would otherwise surprise (existing convention).
- License headers on new files; no `unwrap` in lib code; rustfmt/clippy clean.
- Do not refactor unrelated networking code; `on_response` wiring only if the
  chosen fix requires it (else it stays a recorded follow-up).

### Task 1: RED — discriminating integration test (timer backstop)

**Files:** new `crates/ava-engine/tests/failed_delivery.rs` (or extend the
existing bootstrap/networking integration test module if one fits better).

- [ ] Build the production delivery chain in-process: `AdaptiveTimeoutManager`
  (RealClock, initial timeout ~200ms via `AdaptiveTimeoutConfig`) →
  `ChainRouter` with the chain's sink registered → `OutboundSender` over a
  recording mock `ava_network::Network` → real `Bootstrapper` with 2 beacons.
  Drive `start()`; deliver an `accepted_frontier` reply from beacon A only;
  beacon B never answers (mock network delivers to both).
- [ ] Assert: within ~5× the configured timeout the bootstrapper observes
  `GetAcceptedFrontierFailed(B)` and frontier discovery completes (observable:
  the mock network records the follow-up `get_accepted` broadcast).
- [ ] Record RED output verbatim in the task report (which link failed: no
  timer fire at all vs fired-but-undelivered — instrument with tracing if
  needed to state which).

### Task 2: RED — unsent ⇒ immediate `*Failed` (Go-parity leg)

**Files:** same test file; then `crates/ava-engine/src/networking/sender.rs`.

- [ ] Read the Go oracle: how `sender.Send*` treats nodes missing from
  `network.Send`'s sent-set (immediate `RegisterRequestFail`/synthesized
  failure). Record the exact Go behavior + file:line in the task report.
- [ ] Test: mock `Network` whose send REPORTS beacon B unsent (mirror the real
  `Network::send` contract — if it returns the sent-set, use it; if it is
  fire-and-forget, this task first widens the trait/report path to Go parity,
  minimally). Assert `GetAcceptedFrontierFailed(B)` reaches the bootstrapper
  immediately (no timer wait), matching Go.
- [ ] RED confirmed, then implement the leg in `OutboundSender` and GREEN.

### Task 3: GREEN — fix the timer-backstop link Task 1 exposed

**Files:** whichever single link Task 1 fingered (`timeout.rs` dispatch/clock,
`router.rs` handler capture/push, or engine inbound dispatch of synthesized ops).

- [ ] Minimal fix; Task 1's test GREEN; explain the root cause in one paragraph
  in the task report (this paragraph feeds the AS-BUILT + memory).
- [ ] Full `cargo nextest run -p ava-engine` green; run `-p avalanchers` and
  `-p ava-network` if their wiring was touched.

### Task 4: gates + live re-run (parent plan Tasks 6/7 re-entry)

- [ ] `./scripts/run_task.sh lint` + `lint-saevm` green; workspace test-unit if
  any non-test crate changed.
- [ ] Commit(s) with scope prefix `fix(M9.15):`.
- [ ] Re-run live `mixed_network_single_beacon` (regression guard — expect PASS)
  then `mixed_network` (solo, via `cargo test` — the nextest default profile
  kills at 120s; keep the seed-keyed workdir cleaned before each run).
- [ ] Outcome recorded: PASS ⇒ parent Task 9 (AS-BUILT). New, later rung ⇒
  new Task-8 iteration with its own investigation note.
