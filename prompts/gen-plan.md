Study specs/*.md and generate a detailed plan in plan/*.md with checklists of tasks to implement each of the milestones in order. The tasks should clearly mark dependencies on other tasks to give guidance to subagents to do work in parallel. At each stage, the target avalanchers binary should be buildable and pass tests and exit requirements. Use as many subagents as needed to help construct the plan.
