Study specs-rust/*.md and generate a detailed plan with checklists of tasks to implement each of the milestones in order. The tasks should clearly mark dependencies on other tasks to give guidance to subagents to do work in parallel. At each stage, the target avalanchers binary should be buildable and pass tests and exit requirements.
