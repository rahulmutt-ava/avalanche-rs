Study specs-rust/*.md. Study plan/*.md. 

Find the next set of items that can be implemented in parallel and implement them in distinct worktrees and merge them in when they're done. 

Output <promise>DONE</promise> only when build and tests passes.
