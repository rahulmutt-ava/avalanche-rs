Study specs/*.md. Study plan/*.md. 

Find the next set of items that can be implemented in parallel via subagents and implement them in distinct worktrees and merge them in to the current tree when they're done.

If in the process of implementing new items you find that you need to update the specs/*.md with new findings that you discover during implementation that can simplify the spec or extend the spec with relevant details about choices made, go ahead and update them.

If in the process of implementing new items you find that you need to update the plan/*.md with progress updates after verification of task completion, go ahead and mark the task as completed.
